version: "3"

volumes:
  mysql-data:
    driver: local

services:
  tomcat:
    image: tomcat:jre8-alpine
    ports:
      - "8080:8080"
      - "9009:8009"
    volumes:
      - ./target/jsp-boilerplate-1.0-SNAPSHOT:/usr/local/tomcat/webapps/ROOT
    depends_on:
      - mvn
      - mysql

  mysql:
    image: mysql
    volumes:
      - ./database/data:/docker-entrypoint-initdb.d
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=mydb

  mvn:
    image: maven:alpine
    entrypoint: mvn
    command: install
    volumes:
      - .:/usr/src/app
    working_dir: /usr/src/app
